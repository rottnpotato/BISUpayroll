# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/bisu_payroll?schema=public"

# Authentication Configuration
JWT_SECRET="your-super-secret-jwt-key-here-please-change-in-production"
NEXTAUTH_SECRET="your-nextauth-secret-here-please-change-in-production"
NEXTAUTH_URL="http://localhost:3000"

# Application Configuration
NODE_ENV="development"

# Timezone Configuration
TZ="Asia/Manila"

# Payroll File Encryption (Required for secure file storage)
PAYROLL_ENCRYPTION_SECRET="your-payroll-encryption-secret-change-in-production-must-be-strong"

# Automatic Payroll Generation (Optional - for cron job security)
CRON_SECRET="your-random-cron-secret-token-here"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Instructions:
# 1. Copy this file to .env and fill in your actual values
# 2. Replace 'username' and 'password' with your PostgreSQL credentials
# 3. Ensure PostgreSQL is running on port 5432 (default)
# 4. Create a database named 'bisu_payroll' in PostgreSQL
# 5. Generate secure secrets for production use (especially PAYROLL_ENCRYPTION_SECRET)
# 6. Update NEXTAUTH_URL with your production domain when deploying
# 7. PAYROLL_ENCRYPTION_SECRET is required for payroll file security - use a strong, unique value 